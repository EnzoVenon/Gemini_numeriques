{
  "name": "gemini_numeriques",
  "source": "./src/index.html",
  "staticFiles": {
    "staticOutPath": "./data",
    "staticPath": "./data"
  },
  "jest": {
    "roots": [
      "<rootDir>/src/"
    ]
  },
  "scripts": {
    "start": "parcel --open",
    "build": "parcel build",
    "clean": "rm -rf dist",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@types/three": "^0.148.1",
    "jest": "^29.5.0",
    "parcel": "^2.8.3",
    "parcel-reporter-static-files-copy": "^1.5.0"
  },
  "dependencies": {
    "three": "^0.149.0"
  }
}