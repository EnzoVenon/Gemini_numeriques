{
  "name": "gemini_numeriques",
  "source": "./src/index.html",
  "staticFiles": {
    "staticOutPath": "./data",
    "staticPath": "./data"
  },
  "jest": {
    "roots": [
      "<rootDir>/src/"
    ]
  },
  "scripts": {
    "start": "parcel --open",
    "build": "parcel build",
    "clean": "rm -rf dist",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "parcel": "^2.8.3",
    "parcel-reporter-static-files-copy": "^1.5.0"
  },
  "dependencies": {}
}